---
- hosts: localhost
  connection: local
  gather_facts: no

  tasks:
    - name: terraform init
      command: terraform init arg1
      args:
        chdir: terraform/

    - name: terraform plan
      terraform:
        project_path: "terraform/"
        plan_file: "myplan.tfplan"
        state: planned
      register: plan_results
